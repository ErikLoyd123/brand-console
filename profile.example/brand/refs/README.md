# Reference images

Drop example images here (png / jpg / webp) — screenshots, cards, or photos whose
look you want the imagery pipeline to match. The imagery procedure loads every
image in this folder before composing or curating, and matches palette, spacing,
and mood against them. `brand.yaml` (one level up) holds the explicit rules;
this folder holds the "make it feel like this" examples.
